#pragma once

enum Direction
{
	UP = 1,
	DOWN = 2,
	LEFT = 3,
	RIGHT = 4
};

enum Difficulty
{
	LOW = 300,
	MID = 200,
	HI = 150
};

enum FieldSize
{
	HIGHT = 24,
	WIDTH = 24
};

enum Color
{
	GREEN = 10,
	RED = 12,
	YELLOW = 14,
	BLUE = 11,
	NOKIASTYLE = 32
};